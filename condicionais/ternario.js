const saldo = 1;

// ? equivalente ao if
// : equivalente ao else
const unidade = (saldo === 1 ? "real" : "reais");

console.log(`Você tem R$ ${saldo} ${unidade}!`);

// console.log(`Você tem ${saldo} reais`);
// if (saldo === 1) {
//     console.log(`Voce tem R$ ${saldo} real!`);
// } else {
//     console.log(`Você tem R$ ${saldo} reais!`);
// }