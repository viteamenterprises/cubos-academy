const temIngresso = false;

// ! inverte o valor da variável, se for true -> false mas nao salva na variavel
if (!temIngresso) {
    // se !(não) temIngresso ...
    console.log("Pessoa sem ingresso.")
}
