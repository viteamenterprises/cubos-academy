let x = 10, y = 5;

let resultado = x + y;
console.log(`A soma de ${x} e ${y} é igual a ${resultado}`); //retorna 15

resultado = x - y;
console.log(`A subtração de ${x} e ${y} é igual a ${resultado}`); //retorna 5

resultado = x * y;
console.log(`A multiplicação de ${x} vezes ${y} é igual a ${resultado}`); //retorna 50

resultado = x / y;
console.log(`A divisão de ${x} por ${y} é igual a ${resultado}`); //retorna 2


{
    let x = 10, y = 5;

    let resultado = x + y;
    let mensagem = "A soma de " + x + " e " + y + " é igual a " + resultado;
    console.log(mensagem); //retorna 15

    resultado = x - y;
    mensagem = "A subtração de " + x + " e " + y + " é igual a " + resultado;
    console.log(mensagem); //retorna 5

    resultado = x * y;
    mensagem = "A multiplicação de " + x + " e " + y + " é igual a " + resultado;
    console.log(mensagem); //retorna 50

    resultado = x / y;
    mensagem = "A divisao de " + x + " e " + y + " é igual a " + resultado;
    console.log(mensagem); //retorna 15
}